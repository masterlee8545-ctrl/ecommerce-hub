# 작업 의뢰서 v2: 농가 공급처 찾기 기능 추가

> 이 문서를 ecommerce-hub의 다음 Claude 세션에 *통째로* 붙여넣으세요.
> v1 의뢰서 + 시즌 펄스 연계 + 회사 컨텍스트 제거 + 공유 승인 시스템 반영.

---

## 0. 먼저 읽어야 할 것 (Planner 진입 전 필수)

다음 문서를 모두 읽기. 안 읽고 코드 시작하면 헌법 위반:
- `CLAUDE.md` (헌법, 9대 금지 + 자기검증 5+1 + 하니스 워크플로우)
- `docs/CPS.md`
- `docs/SPEC.md` (현재 38 페이지 / 21 테이블 — 신규 4~5 테이블 추가될 예정)
- `docs/DATA_MODEL.md` (특히 `suppliers`, `products` 테이블)
- `docs/ADR.md` (ADR-001 ~ ADR-011)
- `docs/INFOHUB_INTEGRATION.md`
- `agents/planner.md`, `agents/generator.md`, `agents/evaluator.md`
- `agents/mistakes.md`
- **추가**: `src/app/(app)/research/season-pulse/page.tsx` 와 그 클라이언트 컴포넌트 — 이미 만들어진 시즌 펄스 기능 (입구가 이거)
- **추가**: `src/db/schema/keyword-charts.ts` — 시즌 분석 DB 스키마 (참고)

---

## 1. 추가할 기능 — 한 줄

**"시즌 펄스에서 추천된 시즌 키워드를 상품으로 담은 뒤, 그 상품을 위탁판매해줄 국내 농가/공장 공급처를 찾아 전화하고 결과를 기록한 뒤, 한 곳을 공급처로 확정하는 기능."**

---

## 2. 사용자 진짜 흐름 (이게 본질, 절대 어기지 말 것)

```
[A. 시즌 발굴 — 이미 구현됨]
/research/season-pulse 에서 5점 키워드 발견 ("참외")
   ↓ 🛒 담기 클릭
   ↓ (시즌 메타 함께 저장: peak_month, seasonality_ratio, season_score)
products 테이블에 row 생성 (stage='research')

[B. 공급처 찾기 — 본 의뢰서 핵심]
상품 페이지 안 "공급처 찾기" 탭 클릭
   ↓
시스템이 자동으로 그 상품 키워드/카테고리에 맞는 농가 추천
   ↓
농가 카드: [📞 통화] [⭐ 후보 등록] [❌ 탈락]
   ↓
모바일이면 자동 발신, PC면 번호 표시
   ↓
통화 종료 → 30초 결과 기록 모달 (드롭다운 + 메모 + 다음 액션)
   ↓
가장 좋은 농가 → "이 상품 공급처로 확정" → products 표에 연결
```

**핵심 원칙:**
1. **농가 마스터 메뉴는 보조** — 진입점은 *상품 페이지 안의 "공급처 찾기" 탭*
2. **시즌 펄스 → 상품 → 공급처 찾기**가 한 흐름으로 자연스럽게 이어져야 함
3. 시즌 메타(피크월 등)는 농가 매칭/정렬에도 활용 가능

---

## 3. 사전 합의된 설계

### 3.1 추가/수정할 테이블

| 테이블 | 역할 | 회사ID | 비고 |
|---|---|---|---|
| `vendors` | 국내 농가/공장/제조사 마스터 | ✅ (소유자) | `suppliers`는 1688 중국 전용이라 의미 충돌 → 별도 테이블 |
| `vendor_products` | 농가 ↔ 취급품목 다대다 | ✅ | 정규화된 품목 키워드 |
| `vendor_call_logs` | 통화/카톡/방문 기록 (immutable) | ✅ | result, next_action, next_action_at |
| `product_vendor_candidates` | 상품 ↔ 공급처 후보 | ✅ | status('후보'\|'통화중'\|'견본중'\|'확정'\|'탈락') |
| **🆕 `vendor_access_grants`** | **농가 풀 공유 승인** | ✅ × 2 | 형 결정: 공유하되 승인받은 사람만 |

#### 추가 컬럼 (기존 테이블)

```sql
-- products 테이블에 추가
ALTER TABLE products ADD COLUMN supply_type text;
  -- 값: 'domestic_vendor' | 'overseas_supplier' | 'both' | null
ALTER TABLE products ADD COLUMN primary_vendor_id uuid REFERENCES vendors(id);
  -- 기존 primary_supplier_id 와 별개 (의미 충돌 회피)

-- products 에 시즌 메타 추가 (시즌 펄스에서 담길 때 채워짐)
ALTER TABLE products ADD COLUMN season_peak_month integer;
ALTER TABLE products ADD COLUMN season_prep_month integer;
ALTER TABLE products ADD COLUMN seasonality_ratio numeric(5,2);
ALTER TABLE products ADD COLUMN season_score integer;
  -- 시즌 펄스 5점 스코어링 결과 (5/4/2/1)
```

### 3.2 추가할 화면 5개

| 경로 | 화면 | 핵심 |
|---|---|---|
| `/products/[id]/find-vendor` ⭐ | 상품 안 "공급처 찾기" 탭 | 자동 매칭 + 후보 추가 |
| `/vendors/[id]` | 농가 상세 + 통화 타임라인 | 큼직한 [📞 발신] 버튼 |
| `/vendors/import` | 크롤러 결과 CSV 업로드 | 이름+사업자번호 매칭 → 신규/업데이트 미리보기 |
| `/vendors` | 농가 검색/필터 (보조 메뉴) | 품목/지역/사이트 필터 |
| **🆕 `/vendors/access`** | **공유 풀 신청/승인 관리** | 받은 요청 / 보낸 요청 / 활성 승인 |

통화 기록 폼은 `/vendors/[id]` 안 *모달*로 처리 (별도 페이지 X).

### 3.3 자동 매칭 로직

1. 상품의 `name + category + primary_keyword` 텍스트에서 품목 키워드 추출
2. `vendors`의 `vendor_products` 와 정규화된 키워드 매칭
3. **🆕 시즌 메타 활용**: `products.season_peak_month` 가 현재 ±2개월 안이면 우선 정렬 가중치 +
4. 정렬: 매칭 강도 → 시즌 적합도 → 지역(필터) → 인증(필터) → 최근 통화 활성도

**가공품 → 원료 환원 사전 (보강):**
```
딸기잼 → 딸기
감귤청 → 감귤
참외에이드 → 참외
한우육포 → 한우
```
→ AI 분류 (Claude haiku 한 줄 호출) 또는 정규식 사전. Planner가 결정.

품목 키워드 사전과 매칭 정규식은 `C:\개발\크롤링\aggregate_and_classify.py`의 `PRODUCT_KEYWORDS` 딕셔너리에 검증된 패턴 있음. 그대로 활용.

### 3.4 회사 컨텍스트 → 🚨 **상품 supply_type 으로 결정** (v1 의뢰서에서 변경)

**❌ 변경 전 (잘못된 설계):**
> "바이와이즈(공산품) 컨텍스트에서는 /products/[id]/find-vendor 탭 숨김 (business_type='industrial' 일 때)"

**✅ 변경 후 (올바른 설계):**

| `products.supply_type` | "공급처 찾기"(농가) 탭 | "도매상 찾기"(중국) 탭 |
|---|---|---|
| `domestic_vendor` | ✅ 노출 | ❌ 숨김 |
| `overseas_supplier` | ❌ 숨김 | ✅ 노출 |
| `both` | ✅ 노출 | ✅ 노출 |
| `null` (미정) | ✅ 노출 | ✅ 노출 |

**근거:**
- 형 BUYWISE 운영 현실: 유어밸류도 공산품 할 수 있고, 바이와이즈도 농산물 할 수 있음
- 상용화 시 가입자가 두 카테고리 동시 운영 가능해야 함 (강의생 확장 비전)
- 회사로 분기하면 가입자 제약 → 상품 단위 분기가 자연스러움

**상품 만들 때 `supply_type` 자동 추론:**
- 시즌 펄스에서 🛒 담기 → 키워드로 추론 ("참외" → `domestic_vendor`, "캠핑의자" → `overseas_supplier`)
- 추론 실패 시 → null (사용자가 상품 페이지에서 직접 선택)
- Claude AI 한 줄 분류 호출 (저렴, 정확)

### 3.5 🆕 농가 공유 승인 시스템 (form 결정)

**철학:** 농가 마스터는 공유 가능하되 *승인받은 사람만* 접근.

#### `vendor_access_grants` 테이블 스키마
```sql
CREATE TABLE vendor_access_grants (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  grantor_company_id uuid NOT NULL REFERENCES companies(id),  -- 풀 소유자
  grantee_company_id uuid NOT NULL REFERENCES companies(id),  -- 신청자
  status text NOT NULL DEFAULT 'pending',
    -- 'pending' | 'approved' | 'rejected' | 'revoked'
  requested_at timestamptz NOT NULL DEFAULT now(),
  decided_at timestamptz,
  decided_by_user_id uuid REFERENCES users(id),
  reason text,           -- 신청 사유 (강의생이 형한테)
  notes text,            -- 형이 승인/거절 메모
  UNIQUE (grantor_company_id, grantee_company_id)
);
```

#### RLS 정책 (vendors SELECT)
```sql
-- 자기 회사 vendors 는 항상 조회 가능
-- + 승인된 grantor 회사 vendors 도 조회 가능
CREATE POLICY "vendors_select_own_or_granted" ON vendors
  FOR SELECT TO authenticated
  USING (
    company_id = current_company_id()
    OR company_id IN (
      SELECT grantor_company_id FROM vendor_access_grants
      WHERE grantee_company_id = current_company_id()
        AND status = 'approved'
    )
  );
```

#### vendor_call_logs, product_vendor_candidates 는 격리
- 형이 어떤 농가랑 통화했는지, 어떤 후보 결정했는지 = **개인 자산**
- 다른 가입자는 못 봄 (자기 통화 기록만)

#### 화면 (`/vendors/access`)
```
[받은 요청]                          [내가 보낸 요청]
강의생 김OO — 농가 풀 조회 신청      형 → 강의생 박OO 풀 (승인됨)
  사유: "참외 농가 공부하고 싶어요"   기간: 무제한
  [✅ 승인] [❌ 거절]                 [🚫 회수]

[활성 승인 중]
- 강의생 김OO (참외 농가 접근, 2026-04-15 부터)
```

---

## 4. 데이터 자산 (C:\개발\크롤링)

별도 크롤러 프로젝트(`C:\개발\크롤링`)에서 이미 데이터 모아둠. 임포트 통로(`/vendors/import`)로 받아옴.

| 파일 | 내용 | 행수 |
|---|---|---|
| `all_sellers.csv` | jpsmall + 농가맛집 통합, 분류 컬럼 포함 | 100 |
| `jpsmall_sellers_v2.csv` | 김제지평선몰 입점업체 + 상품 | 69 |
| `nongasil_sellers.csv` | 부안 농가맛집 입점업체 + 상품 + 사업자번호 | 31 |
| `malls_master.csv` | 농사로 공식 88개 지자체몰 마스터 | 88 |
| `malls_probe.csv` | 88개 몰 플랫폼 자동분류 + 입점업체 페이지 후보 | 88 |

`all_sellers.csv` 컬럼: `site, slug, 업체명, 분류, 분류_근거, 대표자명, 연락처, 휴대전화, 주소, 사업자번호, product_count, product_keywords_all, products_top10, url`

분류 값: `1차_농가 | 1차_법인 | 1차_상품기반 | 1차_혼합 | 농협 | 농협가공 | 가공유통 | 가공_상품기반 | 불명`

**크롤러는 이커머스허브 안에 흡수하지 말 것.** 가끔 1번씩 돌리는 외부 작업이라 분리 유지. 임포트 시점에 CSV → DB 매핑 + 중복 dedup(이름+사업자번호+주소).

**임포트 매칭 키 권장:**
1. 사업자번호 (있는 경우 정규화 후 정확 매칭)
2. 업체명 + 주소 정규화 fingerprint
3. 업체명 + 대표자명

**신뢰도 마킹** (헌법 §4):
- 크롤러 추출 필드: 🟡 `estimated`
- 사용자 통화 후 확정한 값: ✅ `confirmed`
- 추출 실패/빈 값: ❓ `unknown`
- 각 컬럼에 `*_confidence` 동반 추가
- `vendors.source_site` 컬럼으로 발굴 출처 추적

---

## 5. 농산물 위탁판매 ↔ 기존 6단계 매핑

새 단계 만들지 말고 헌법 §3.1 6단계 그대로 재사용:

| 공산품 단계 | 농산물 의미 |
|---|---|
| `research` | 시장/품목 발굴 (시즌 펄스 + 셀록홈즈 카테고리) |
| `sourcing` | 농가 컨택 ← **본 기능 핵심** |
| `importing` | 견본 수령 + 계약 |
| `listing` | 플랫폼 등록 |
| `active` | 판매중 |
| `branding` | 브랜딩 |

→ ADR-012에 이 매핑 결정 사항으로 기록.

---

## 6. 진행 순서 (헌법 §3 하니스 워크플로우)

### Phase A — Planner (지금 시작, 코드 작성 금지)

산출물:
- `docs/SPEC.md` 변경분 — §4.x에 화면 5개 추가, §6 자동화 트리거에 추가
- `docs/DATA_MODEL.md` 변경분 — 테이블 5개 신규 + products 컬럼 추가
- `docs/ADR-012.md` 신규 — "농가 vendors 분리 + 6단계 매핑 + supply_type 컬럼"
- `docs/ADR-013.md` 신규 — "농가 공유 승인 시스템 (vendor_access_grants)"
- 작업 분할 목록 — Generator가 만들 PR 단위로 5~7개

**작성 후 사용자(이재홍 대표)에게 P-9 비개발자 톤으로 보고하고 승인 받을 것. 코드 작성 시작하지 말 것.**

### Phase B — Generator (Phase A 승인 후)

작업 분할 1개씩 처리. 각 PR마다 자기검증 5+1 + Evaluator 서브에이전트 호출.

**PR 분할 예상:**
1. 마이그레이션 + 스키마 (vendors, vendor_products, vendor_call_logs, product_vendor_candidates, vendor_access_grants, products 컬럼 추가)
2. lib/vendors/ — 자동 매칭 알고리즘 + 임포트 헬퍼
3. API: `/api/vendors/*` route
4. UI: `/vendors/import` (크롤링 CSV 업로드)
5. UI: `/products/[id]/find-vendor` 탭 ⭐
6. UI: `/vendors/[id]` + 통화 모달
7. UI: `/vendors`, `/vendors/access`
8. 시즌 펄스 → 상품 만들 때 시즌 메타 자동 저장 (작은 패치)

---

## 7. 사용자가 아직 결정 안 한 2가지 (Planner가 보고 시 같이 물을 것)

1. **자동 매칭 추가 필터** — "지역(전북 우선)", "인증(유기농 우선)" 1차 출시 포함 여부
2. **사이소(경북 23개몰) 토큰** — 사용자가 안동장터 DevTools에서 Authorization 잡아주면 23개 사이트 일괄 추가. Phase B 도중 처리

**결정된 사항** (v1 → v2):
- ✅ products 공급처 컬럼 → 새 `primary_vendor_id` 추가 (suppliers 와 분리)
- ✅ 회사 컨텍스트 분기 → 상품 `supply_type` 으로 변경
- ✅ 농가 공유 정책 → 승인제 (vendor_access_grants)
- ✅ 시즌 펄스 연계 → products 에 시즌 메타 컬럼 추가

---

## 8. 절대 하지 말 것

- ❌ `suppliers` 테이블에 농가 컬럼 추가 (P-4 ADR 위반)
- ❌ `/vendors` 메뉴를 메인 진입점으로 설계 (사용자 진짜 흐름: 시즌 펄스 → 상품 → 공급처 찾기)
- ❌ 더미 농가 데이터 시드 (P-1) — 임포트는 실제 CSV 사용
- ❌ 통화 기록을 `vendor_call_logs` 외 다른 테이블에 흩어 두기
- ❌ Planner 단계 건너뛰고 코드부터 짜기
- ❌ 회사 종류(business_type)로 탭 노출/숨김 결정 (가입자 제약 발생)
- ❌ 모든 사용자에게 모든 vendors 공개 (승인제 필수)

---

## 9. 회사 컨텍스트 (변경됨)

- 본 기능은 **모든 회사**에서 활성 (유어밸류, 바이와이즈, 미래 가입자 모두)
- 상품의 `supply_type` 으로만 탭 노출 결정
- `vendors`, `vendor_call_logs`, `product_vendor_candidates` 모두 `company_id` 강제 + RLS 적용
- `vendors` 만 `vendor_access_grants` 통한 *공유 SELECT* 허용 (RLS 정책)
- 통화 기록, 후보 결정은 항상 격리 (자기 회사만 조회)
- 상용화 비전: 가입자가 농산물/공산품 동시 운영 가능 (강의생 확장 포함)

---

## 10. 시즌 펄스 연계 — 한 흐름으로 자연스럽게

**이미 구현된 시즌 펄스:**
- `src/app/(app)/research/season-pulse/page.tsx`
- `src/app/(app)/research/season-pulse/season-pulse-client.tsx`
- `src/app/api/season-recommendations/route.ts`
- `src/lib/sellochomes/season-analyzer.ts`
- `src/db/schema/keyword-charts.ts`

**연계 작업 (작은 패치):**

1. **시즌 펄스 카드의 🛒 담기 동작 수정**
   - 현재: `quickAddToBasketAction(formData)` 로 name + memo 만 저장
   - 변경: 추가 필드 (peak_month, prep_month, seasonality_ratio, season_score) 함께 저장

2. **`quickAddToBasketAction` 또는 신규 액션 추가**
   - 시즌 메타 받아서 products 에 채워넣기

3. **상품 상세 페이지에서 시즌 메타 표시**
   - "📅 6월 피크 · 시즌성 7.5x · 5점 추천" 같은 라벨
   - 공급처 찾기 탭에서 "현재 시즌 농가 우선" 정렬 옵션

---

**이상.** 이 메시지 전체를 ecommerce-hub 다음 Claude 세션 첫 메시지로 붙여넣으세요.
